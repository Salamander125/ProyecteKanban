<Window x:Class="ToDoListTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ToDoListTest"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0">
            <TextBlock FontSize="20" FontWeight="Bold" Margin="0,10,0,10">Lista de Tickets</TextBlock>
            <Grid Margin="20" Width="300" Height="304">
                <ListView Name="ListaTIckets" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>

                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Button Click="AccesoNuevaVentana"
                            MinWidth="50"
                            MinHeight="30"
                            Margin="-1,-1,0,0"
                            Background="{Binding ColorPrioritat}">
                                <StackPanel>
                                    <TextBlock FontWeight="Bold" Text="{Binding Title}"/>
                                    <TextBlock Text="{Binding Description}" TextWrapping="Wrap"/>
                                    <TextBlock Text="{Binding Codigo}" TextWrapping="Wrap"/>
                                </StackPanel>
                            </Button>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </StackPanel>
        <StackPanel Grid.Column="1">
            <TextBox Name="IntroTitle" Height="20" Width="100" Margin="10" HorizontalAlignment="Left"></TextBox>
            <TextBox Name="IntroDescription" Height="70" Margin="10"></TextBox>
            <Button Width="90" Margin="0,10,0,0" Click="Button_Click">Añadir tasca</Button>
        </StackPanel>
    </Grid>
</Window>
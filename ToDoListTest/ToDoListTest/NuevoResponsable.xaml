<Window
    x:Class="ToDoListTest.NuevoResponsable"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Añadir Responsable"
    Width="400"
    Height="480"
    Background="#F5F5F5"
    WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="#555" />
            <Setter Property="Margin" Value="0,0,0,5" />
        </Style>

        <Style x:Key="InputStyle" TargetType="Control">
            <Setter Property="Padding" Value="8" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="BorderBrush" Value="#DDD" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Height" Value="40" />
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="5" />
                </Style>
            </Style.Resources>
        </Style>
    </Window.Resources>

    <Border
        Margin="20"
        Padding="30"
        Background="White"
        CornerRadius="12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,25">
                <TextBlock
                    FontSize="20"
                    FontWeight="Bold"
                    Foreground="#333"
                    Text="Nuevo Responsable" />
                <Separator
                    Height="2"
                    Margin="0,10,0,0"
                    Background="#EBECF0" />
            </StackPanel>

            <StackPanel Grid.Row="1" Margin="0,0,0,15">
                <TextBlock Style="{StaticResource LabelStyle}" Text="Nombre de Usuario" />
                <TextBox Name="txtUsername" Style="{StaticResource InputStyle}" />
            </StackPanel>

            <StackPanel Grid.Row="2" Margin="0,0,0,15">
                <TextBlock Style="{StaticResource LabelStyle}" Text="Contraseña" />
                <PasswordBox Name="txtPassword" Style="{StaticResource InputStyle}" />
            </StackPanel>

            <CheckBox
                Name="chkIsAdmin"
                Grid.Row="3"
                Margin="0,10,0,0"
                VerticalContentAlignment="Center">
                <TextBlock
                    Margin="5,0,0,0"
                    FontSize="13"
                    Foreground="#555"
                    Text="Conceder permisos de Administrador" />
            </CheckBox>

            <StackPanel
                Grid.Row="5"
                HorizontalAlignment="Right"
                Orientation="Horizontal">
                <Button
                    Width="90"
                    Height="40"
                    Margin="0,0,10,0"
                    Background="Transparent"
                    BorderBrush="#CCC"
                    Click="Cancelar_Click"
                    Content="Cancelar">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8" />
                        </Style>
                    </Button.Resources>
                </Button>

                <Button
                    Width="140"
                    Height="40"
                    Background="#0079BF"
                    BorderThickness="0"
                    Click="GuardarResponsable_Click"
                    Content="Crear Usuario"
                    FontWeight="Bold"
                    Foreground="White">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8" />
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>